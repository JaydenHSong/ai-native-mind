# bkit internal state (tool-generated, not for sharing)
.bkit/

# Obsidian local/workspace state (keep shared config only)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/graph.json
.obsidian/hotkeys.json
.obsidian/plugins/

# Claude/agent local state
.claude/

# Obsidian scratch canvas (default "Untitled" names — do not commit)
무제.canvas
Untitled.canvas

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Logs
*.log

# Secrets & local env (public repo — never commit real values)
.env
.env.*
!.env.example

# Node (docs tooling / local installs)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Build / site generators (if added later)
dist/
.cache/
.turbo/
.vercel/
web/wiki-data.json

# Keys & keystores (if ever added by mistake)
*.pem
*.p12
*.pfx
id_rsa
id_ed25519
*.key
*.crt
*.cer
known_hosts.bak

# Local DB / backups
*.sqlite
*.sqlite3
*.db
*.bak
*~

# OS / tooling extras
.AppleDouble
.LSOverride
*.orig
.vercel
