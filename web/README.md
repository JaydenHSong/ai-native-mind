# ai-native-mind web

이 폴더는 `ai-native-mind` 위키와 분리된 **독립 웹 앱 루트**입니다.

- Vercel 연결 대상 루트로 사용
- `wiki/`, `raw/`, `templates/` 등 위키 지식 구조와 분리
- 위키 정리/인덱싱 시 이 폴더 내용은 위키 본문으로 취급하지 않음

필요하면 다음 단계에서 여기에 Next.js / Astro / Vite 같은 실제 웹 프로젝트를 초기화하면 됩니다.
